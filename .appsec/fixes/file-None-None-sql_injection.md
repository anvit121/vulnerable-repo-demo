# Security Fix — sql_injection

## Vulnerability
The code is vulnerable to SQL injection because it directly inserts user-supplied input into the SQL query, allowing an attacker to manipulate the query. This can be fixed by using parameterized queries.

## Vulnerable code
```python
query = f"SELECT * FROM users WHERE email = '{email}'"
```

## Fixed code
```python
query = "SELECT * FROM users WHERE email = %s"; cursor.execute(query, (email,))
```

## Why this works
The fix uses a parameterized query, where the email is passed as a parameter to the execute method, preventing an attacker from injecting malicious SQL code.

---
Auto-generated by AutoFix AppSec AI
