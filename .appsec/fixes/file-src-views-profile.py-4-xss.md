# Security Fix — xss

## Vulnerability
The code is vulnerable to XSS because it directly concatenates user-controlled input into HTML without proper escaping, allowing an attacker to inject malicious scripts. This can be fixed by using a templating engine that automatically escapes user input.

## Vulnerable code
```python
template = "<h1>Welcome " + username + "</h1>"
```

## Fixed code
```python
from jinja2 import Template; template = Template('<h1>Welcome {{ username }}</h1>'); rendered_template = template.render(username=username)
```

## Why this works
The fix uses the Jinja2 templating engine, which automatically escapes user input, preventing XSS attacks. The username variable is now passed to the template.render method, ensuring it is properly escaped.

---
Auto-generated by AutoFix AppSec AI
