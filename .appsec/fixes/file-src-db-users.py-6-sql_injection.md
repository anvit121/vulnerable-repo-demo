# Security Fix — sql_injection

## Vulnerability
The code is vulnerable to SQL injection because it directly concatenates user input into a SQL query, allowing an attacker to inject malicious SQL code. This can be fixed by using parameterized queries.

## Vulnerable code
```python
query = "SELECT * FROM users WHERE id = " + user_id
```

## Fixed code
```python
query = "SELECT * FROM users WHERE id = %s"; cursor.execute(query, (user_id,))
```

## Why this works
The fix uses a parameterized query, where the user input is passed as a parameter to the query, preventing an attacker from injecting malicious SQL code. This is a secure way to construct SQL queries with user input.

---
Auto-generated by AutoFix AppSec AI
