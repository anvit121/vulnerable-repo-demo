# Security Fix — xss

## Vulnerability
The code is vulnerable to XSS because it directly concatenates user-controlled input into HTML without proper escaping, allowing an attacker to inject malicious scripts. This can be fixed by using a templating engine that automatically escapes user input.

## Vulnerable code
```python
template = "<h1>Welcome " + username + "</h1>"
```

## Fixed code
```python
from flask import escape; template = "<h1>Welcome " + escape(username) + "</h1>"
```

## Why this works
The fix uses Flask's escape function to properly escape the username variable, preventing any malicious scripts from being injected into the HTML.

---
Auto-generated by AutoFix AppSec AI
